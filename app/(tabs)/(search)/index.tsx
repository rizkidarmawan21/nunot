import Container from '@/components/Container';
import { Image, StyleSheet, Platform, View, Text } from 'react-native';

export default function SearchScreen() {
  return (
    <Container>
      <Text>Search with Container</Text>
    </Container>
  );
}